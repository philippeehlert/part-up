import * as React from 'react';

interface Props {
    match?: Object;
    location?: Object;
    history?: Object;
}

export class Home extends React.Component<Props, {}> {

    public render() {
        return (
            <React.Fragment />
        );
    }
}
