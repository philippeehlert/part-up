import * as React from 'react';

import {
    NavigationBar,
} from 'components';

export default class DevelopmentNavigation extends React.Component {
    render() {
        return (
            <NavigationBar>
                <span>Partup React Development Environent</span>
            </NavigationBar>
        );
    }
}